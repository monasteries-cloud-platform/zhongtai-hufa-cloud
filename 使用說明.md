# 精舍選擇＋登入／精舍後台管理 模組　使用說明

版本：v1.1（2026-09-26）

- v1.1（2026-09-26）：配合 `login.html` v1.1，登入成功後改為直接進入 `report-admin.html`（提報名單資料組操作），同步更新檔案用途表與操作流程說明。
- v1.0（2026-09-16）：初版。

## 這個模組是什麼

這是護法會雲端系統的**共用前端登入模組**，包含 3 個檔案：

| 檔案 | 用途 |
|---|---|
| `config.js` | 共用設定與共用函式（Web App 網址、呼叫後端 API、登入狀態存取），`login.html` 與 `temple-admin.html` 都會用到 |
| `login.html` | 精舍登入頁面，直接輸入精舍代碼與密碼登入 |
| `temple-admin.html` | 「精舍後台管理」頁面（從 `report-admin.html` 上方的「回精舍後台管理」連結進入），可以修改密碼、設定 6 種試算表 ID |

日後提報系統、報到系統的正式頁面，都可以直接引用 `config.js`，共用同一套登入狀態與 API 呼叫邏輯，不需要重寫。

## 安裝方式

1. 確認「系統管理後台」模組（`系統管理_01～04.gs`）已經部署為 Web App，並且已經拿到網址（例如
   `https://script.google.com/macros/s/xxxxx/exec`）。
2. 打開 `config.js`，把第 13 行的 `WEB_APP_網址` 改成您實際部署拿到的網址：
   ```javascript
   const WEB_APP_網址 = "您的實際網址";
   ```
3. 把 `config.js`、`login.html`、`temple-admin.html` 這 3 個檔案放在同一個資料夾（例如您的
   GitHub Pages 網站資料夾），不要分開放，因為 `login.html` 和 `temple-admin.html` 都是用
   `<script src="config.js"></script>` 的方式引用設定檔。
   登入成功後會導向 `report-admin.html`，所以 `report-admin.html` 也必須放在同一個資料夾，否則登入後會找不到頁面。
4. 部署到 GitHub Pages（或其他靜態網站空間）後，用瀏覽器打開 `login.html` 即可開始使用。

## 操作流程

1. 使用者打開 `login.html`，輸入精舍代碼與密碼，點擊「登入」。
   - 新精舍尚未修改密碼的話，預設密碼是 `1234`。
2. 登入成功後，會自動跳轉到 `report-admin.html`（提報名單資料組操作頁面），這是資料組平時最常用的頁面。
   需要修改密碼或設定試算表 ID 時，點頁面上方的「回精舍後台管理」連結，進入 `temple-admin.html`（精舍後台管理頁面）。
3. 在精舍後台管理頁面可以：
   - **修改密碼**：需要先輸入目前密碼才能修改，新密碼要輸入兩次以確認一致。是否修改由精舍自行決定，
     不修改也可以繼續使用預設密碼 1234。
   - **設定試算表 ID**：填入 6 種試算表 ID（資料庫、提報名單、出席回報、報到名單、封存備查、學員基本資料）。
     只需要填寫要修改或新增的欄位，沒有填寫的欄位不會被清空、會維持原本設定。
4. 點擊右上角「登出」可以清除登入狀態並返回登入頁面。

## 測試方式

本模組已經用 Node.js（jsdom 模擬瀏覽器環境）跑過 10 個情境的自動化測試，涵蓋：

- 密碼錯誤時正確擋下、不會存入登入狀態
- 密碼正確時成功登入、正確跳轉到後台管理頁面
- 未登入時打開後台管理頁面會自動導回登入頁面
- 登入後正確顯示精舍名稱
- 修改密碼：舊密碼錯誤、兩次新密碼不一致、正確修改成功，這 3 種狀況都正確處理
- 設定試算表 ID：完全空白時擋下、正確儲存後畫面即時顯示最新設定
- 登出後正確清除登入狀態並導回登入頁面

這是開發端的邏輯測試，確保程式碼本身沒有明顯錯誤；正式使用前，仍建議法師比照模組 1 的方式，
實際用瀏覽器打開 `login.html`，搭配一個測試用精舍代碼，親自跑一次完整流程（登入 → 改密碼 →
設定試算表ID → 登出 → 用新密碼再登入一次），確認畫面顯示與操作體驗都符合預期。

## 已知設計（延續 Phase 3 架構決議，非本模組新增）

- 修改密碼後，目前分頁的登入狀態不會被強制登出，會維持到 2 小時效期自然到期為止，只有「用舊密碼
  重新登入」才會被擋下。這是先前已確認的設計，本模組直接沿用，未額外處理。
- 登入狀態存放在瀏覽器的 sessionStorage，關閉分頁後登入狀態就會消失，需要重新登入。
